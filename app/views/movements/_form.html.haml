= form_with model: movement do |f|
  - if movement.errors.any?
    %div{style: "color: red"}
      %h2
      = pluralize(product.errors.count, "error")
      prohibited this product from being saved:
      %ul
      - movement.errors.each do |error|
        %li= error.full_message
      .mb-3
      = f.label :description, class: "form-label", for: "description"
      = t.text_field :description, class: "form-control"
      .mb-3
      = f.label :description, class: "form-label", for: "Movement type"
      = f.select :movement_type, [["Selecione..", ""], ["Entrada", 1], ["Saída", 2]], { include_blank: false }, class: "form-control"
      .mb-3.col-md-2
      = f.label :value, class: "form-label"
      = f.text_field :value, id: "currency", class: "form-control", placeholder: "0.00"
      .mb-3.d-flex.justify-content-end
    = f.submit value: "Salvar", class: "btn btn-primary"